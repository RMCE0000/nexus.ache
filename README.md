# nexus.ache

Starter website for the `nexus.ache` holding company.

## Local development

```bash
npm install
npm run dev
```

Then open the local Vite URL shown in terminal.

## Build for production

```bash
npm run build
```

The production build is generated in `dist/`.
